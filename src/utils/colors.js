import colors from "@juel/hooks/colors";

export const lightColors = {
  main: colors.purple,
  sec: colors.purple,
  neu: colors.gray,
};
export const darkColors = {
  main: colors.green,
  sec: colors.rose,
  neu: colors.gray,
};
